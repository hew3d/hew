{
  "name": "hew-web",
  "private": true,
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "pnpm --dir ../../app build && rm -rf dist && cp -R ../../app/dist dist"
  }
}
