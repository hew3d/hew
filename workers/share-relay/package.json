{
  "name": "share-relay",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker: one-shot ciphertext dead-drop for Hew's \"Open on Phone\" handoff.",
  "type": "module",
  "scripts": {
    "test": "node --test src/*.test.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
