# wasm-pack build output (generated by `npm run build` / the wasm-pack
# nodejs command) — mirrors app/src/wasm/pkg/, never committed.
/pkg-node/

# Standard Node dependency cache (this package has zero npm deps today, but
# ignore it defensively in case that changes).
/node_modules/
